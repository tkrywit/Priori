<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_red_700</item>
        <item name="colorPrimaryDark">@color/material_grey_500</item>
        <item name="colorAccent">@color/material_blue_grey_800</item>
        <item name="floatingActionButtonStyle">@style/AppTheme.Fab</item>
    </style>

    <style name="AppTheme.Fab" parent="FloatingActionButton">
        <item name="floatingActionButtonColor">@color/material_red_A400</item>
    </style>

</resources>
